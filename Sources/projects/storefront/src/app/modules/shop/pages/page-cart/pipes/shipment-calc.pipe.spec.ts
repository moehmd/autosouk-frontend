import { ShipmentCalcPipe } from './shipment-calc.pipe';

describe('ShipmentCalcPipe', () => {
  it('create an instance', () => {
    const pipe = new ShipmentCalcPipe();
    expect(pipe).toBeTruthy();
  });
});
